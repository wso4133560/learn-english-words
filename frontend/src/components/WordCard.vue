<template>
  <div class="word-card-container" role="button" tabindex="0" :aria-label="isFlipped ? '隐藏释义' : '查看释义'" @click="handleFlip" @keydown.enter.stop.prevent="handleFlip" @keydown.space.stop.prevent="handleFlip"><div :class="['word-card',{flipped:isFlipped}]"><div class="card-face card-front"><span class="face-label">WORD / 单词</span><div class="word">{{ word }}</div><div class="hint">点击卡片查看释义 <span>↗</span></div></div><div class="card-face card-back"><span class="face-label">MEANING / 释义</span><div class="word-small">{{ word }}</div><div class="meaning">{{ meaning }}</div><div class="hint">点击卡片返回单词</div></div></div></div>
</template>
<script setup lang="ts">interface Props { word:string; meaning:string; isFlipped:boolean }; defineProps<Props>(); const emit=defineEmits<{flip:[]}>(); const handleFlip=()=>emit('flip')</script>
<style scoped>
.word-card-container{perspective:1200px;cursor:pointer;user-select:none;width:min(560px,calc(100vw - 40px))}.word-card{position:relative;height:350px;transition:transform .7s cubic-bezier(.2,.75,.25,1);transform-style:preserve-3d}.word-card.flipped{transform:rotateY(180deg)}.card-face{position:absolute;inset:0;backface-visibility:hidden;border-radius:28px;padding:38px;display:flex;flex-direction:column;align-items:flex-start;justify-content:center;overflow:hidden;background:linear-gradient(145deg,#f8f4ec,#e7e1d7);color:#1a1d26;box-shadow:0 30px 70px rgba(0,0,0,.28)}.card-face::after{content:'';position:absolute;width:220px;height:220px;border-radius:50%;right:-70px;bottom:-95px;background:rgba(242,123,91,.24)}.card-back{transform:rotateY(180deg);background:linear-gradient(145deg,#2b3448,#1f2636);color:#f7f4ee}.face-label{font-size:11px;letter-spacing:.18em;font-weight:800;opacity:.55}.word{font-size:clamp(48px,7vw,76px);letter-spacing:-.07em;font-weight:750;margin-top:35px;word-break:break-word}.word-small{font-size:28px;font-weight:700;margin-top:30px}.meaning{font-size:20px;line-height:1.6;margin-top:16px;max-width:100%;overflow-y:auto;min-height:0;flex:1;padding-right:8px;user-select:text;z-index:1}.card-back{justify-content:flex-start;padding-bottom:65px}.card-back .word-small{margin-top:14px}.hint{position:absolute;left:38px;bottom:30px;font-size:12px;opacity:.54;z-index:1}.hint span{font-size:16px;margin-left:6px}.card-back .hint{color:#fff}@media(max-width:560px){.word-card{height:300px}.card-face{padding:26px}.hint{left:26px;bottom:24px}.word{margin-top:24px}.meaning{font-size:21px}}
</style>
